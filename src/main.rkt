#lang rosette

(require "evm.rkt"
         "core.rkt"
         "utils/decoder.rkt")

; (define (prepare-environment filename)
  ; (define code (read-code filename))
  ; )

(define (main filename) '())
